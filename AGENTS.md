# Repository Notes

- Canonical skill source: `.agents/skills/wiki-garden/`
- Distribution mirror: `skills/wiki-garden/`
- When changing the skill package, update the canonical `.agents` copy first, then refresh the top-level `skills` mirror before publishing.
- User-facing install and usage entrypoints are documented in `README.md`.
